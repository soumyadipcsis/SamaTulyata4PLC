PROC FLOOR_BASIC;
VAR
    INPUT    : INT;
    THRESH   : INT;
    OUTPUT   : BOOL;
BEGIN

STEP 'FLOOR_COMPARISON'
    IF INPUT < THRESH THEN
        OUTPUT := TRUE;
    ELSE
        OUTPUT := FALSE;
    END
ENDSTEP

END.
